import { Header } from "./styles";

const Navbar = () => {
  return (
    <Header>
      <nav>
        <img src="/assets/logo.webp" alt="Shopper Logo" />
      </nav>
    </Header>
  );
};

export default Navbar;
